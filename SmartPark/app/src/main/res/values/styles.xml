<resources>

    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryDark">@color/background_dark</item>
        <item name="colorAccent">@color/cardview_light_background</item>
        <item name="cardViewStyle">@style/MyCardView</item>
    </style>

    <style name="MyCardView" parent="CardView">
        <item name="cardElevation">8dp</item>
        <item name="cardCornerRadius">16dp</item>
        <item name="cardBackgroundColor">@color/white</item> <!-- Set your desired color -->
    </style>

</resources>
